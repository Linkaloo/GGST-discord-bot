import Discord from "discord.js";
import * as requests from "../requests";

export const characterHandler = async (message) => requests.getCharacters({});

export const addCharacterHandler = async (message) => {

};
